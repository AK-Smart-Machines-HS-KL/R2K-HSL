FIELD LIMITS: X is between -4.5 and 4.5. Y is between -3.0 and 3.0.
Opponent Goal: X=4.5 (Always attack this direction).
Own Goal: X=-4.5 (Never shoot this way).

VALID OUTPUT LINES (one line per blue bot):
1. blue_1 move to (X, Y)
2. blue_1 cover the goal line at (-4.0, Y)
3. blue_1 kick
4. blue_1 hold position

STRICT LAWS:
- STAY INSIDE FIELD AT ALL TIMES. Never output Move targets with X outside [-4.5, 4.5] or Y outside [-3.0, 3.0]. If the ball is near a boundary, approach from inside the field, not from outside.
- NO OWN GOALS: Never kick if you are between the ball and your own goal (X=-4.5).
- GOAL LINE COVER: One bot covers the goal line: it moves to X=-4.0 and follows the ball's Y position. Never give it a fixed Y.
- RESTART EXCEPTION: During a restart (kick-in, goal kick, corner kick-in), the restart bot may move up to 1m outside the field boundary to approach the ball from behind. After the kick, return inside immediately.
- DO NOT COPY EXAMPLES: Use the ball and bot positions from the INPUT above. Never repeat example coordinates.
- ONE LINE PER BOT: Output exactly one line for EVERY blue bot in the INPUT (blue_1, blue_2, blue_3, ...). Never use the same bot twice.
